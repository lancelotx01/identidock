identidock-1:
  build: ./apps/identidock-1
  expose:
    - "9090"
  environment:
    ENV: PROD
  volumes:
    - ./apps/identidock-1:/app
  links:
    - dnmonster
    - redis

dnmonster:
  image: amouat/dnmonster:1.0

redis:
  image: redis:3.0
