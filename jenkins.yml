identidock-1:
  build: ./apps/identidock-1
  expose:
    - "9090"
  volumes:
    - ./apps/identidock-1:/app
  environment:
    ENV: PROD
  links:
    - dnmonster
    - redis

dnmonster:
  image: amouat/dnmonster:1.0

redis:
  image: redis:3.0
